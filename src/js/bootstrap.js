import * as bootstrap from 'bootstrap'

// most bootstrap components use data-attributes to handle their functionality
// but if you need handle through JS here is an example

// popover example
// document.querySelectorAll('[data-bs-toggle="popover"]').forEach((popover) => {
//   new bootstrap.Popover(popover)
// })
